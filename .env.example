# shellcheck shell=bash
# shellcheck disable=SC2034
# ============================================================================
# BingXtg Trading Bot Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# Required fields must be filled, optional fields have working defaults

# ============================================================================
# REQUIRED: Telegram API Configuration
# ============================================================================
# Get your API credentials from https://my.telegram.org/apps
API_ID=123456
API_HASH=your_telegram_api_hash_here

# ============================================================================
# REQUIRED: BingX API Configuration
# ============================================================================
# Get your API credentials from BingX account settings
APIKEY=your_bingx_api_key_here
SECRETKEY=your_bingx_secret_key_here

# ============================================================================
# REQUIRED: Trading Configuration
# ============================================================================
# Trading parameters for position sizing and risk management
LEVERAGE=20                    # Leverage multiplier (e.g., 10, 20, 50)
SL=-0.5                        # Stop Loss percentage (negative value)
TP=0.2                         # Take Profit percentage (positive value)
START_BALANCE=10.0             # Initial balance for simulation mode (USDT)
MIN_ORDERS_TO_HIGH=20          # Minimum orders before using high win rate
MAX_PERCENT=0.3                # Maximum position size as % of balance

# ============================================================================
# REQUIRED: Django Configuration
# ============================================================================
# Generate a secure secret key for production (use: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=True              # Set to False in production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# ============================================================================
# AI MODEL CONFIGURATION
# ============================================================================
# Choose your AI backend by configuring the appropriate model paths below.
# 
# OPTION 1: HuggingFace Transformers (RECOMMENDED - Higher Accuracy)
# - Requires trained HF models in ai/models/signal_classifier and ai/models/ner_extractor
# - Better accuracy (93%+) but slightly slower inference (~100ms)
# - To use: Ensure the directories exist and contain trained models
# - To disable: Delete or rename the model directories
#
# OPTION 2: Legacy spaCy Models (Fallback)
# - Requires trained spaCy models in ai/models/is_signal_model, etc.
# - Faster inference (~50ms) but lower accuracy (~85%)
# - Automatically used if HF models are not found
#
# The system will automatically prefer HF models if they exist.
# ============================================================================

# HuggingFace Model Paths (RECOMMENDED - comment out to disable HF models)
HF_CLASSIFIER_MODEL_PATH=ai/models/signal_classifier
HF_NER_MODEL_PATH=ai/models/ner_extractor

# Legacy spaCy Model Paths (Fallback - used if HF models not found)
# These are only needed if you're NOT using HuggingFace models
# IS_SIGNAL_MODEL_PATH=ai/models/is_signal_model
# DIRECTION_MODEL_PATH=ai/models/direction_model
# NER_MODEL_PATH=ai/models/ner_model
