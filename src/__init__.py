"""RegexGenAI Python package root.

This marks the `src` directory as a package so subpackages like
`src.core` and `src.telegram` can be imported.
"""
# Intentionally empty; subpackages are discovered via their own __init__.py files.
